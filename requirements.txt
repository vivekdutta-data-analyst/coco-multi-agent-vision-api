# --- Core ML / training (Phase 1) ---
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.26.0
pandas>=2.2.0
Pillow>=10.2.0
matplotlib>=3.8.0
scikit-learn>=1.4.0

# --- Agentic workflow (Phase 2) ---
langgraph>=0.2.0
langchain-core>=0.3.0
anthropic>=0.34.0        # default multimodal LLM provider
# openai>=1.30.0          # uncomment if you swap call_multimodal_llm() to OpenAI/GPT-4o
# google-generativeai>=0.7.0   # uncomment for Gemini instead

# --- API (Phase 3) ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # required by FastAPI for UploadFile/File

# --- Notebook / dataset download ---
jupyter>=1.0.0
kaggle>=1.6.0
